/**
 * Mid-week coaching feature flags — flip to re-enable participant form, emails, banner, and hub.
 */
export const MID_WEEK_COACHING_ENABLED = false;

/** Leadership coaching hub (/performance-check-ins). Kept in codebase; gated here. */
export const MID_WEEK_COACHING_HUB_ENABLED = false;
